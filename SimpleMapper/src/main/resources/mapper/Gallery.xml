<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.simplecoding.simplemapper.gallery.mapper.GalleryMapper">
	<select id="selectGalleryList"
	        parameterType="Criteria"
	        resultType="GalleryVO"
	>
		SELECT * FROM TB_GALLERY
		WHERE GALLERY_TITLE LIKE '%' || #{searchKeyword} || '%'
	    OFFSET #{offset} ROWS
	    FETCH NEXT #{size} ROWS ONLY
	</select>
	
	<select id="selectGalleryListTotCnt"
			parameterType="Criteria"
			resultType="int"
	>
		SELECT COUNT(*) FROM TB_GALLERY
		WHERE GALLERY_TITLE LIKE '%'|| #{searchKeyword} ||'%'
	</select>
	
	<insert id="insert" parameterType="GalleryVO">
		INSERT INTO TB_GALLERY(UUID, 
								GALLERY_TITLE, 
								GALLERY_FILE_URL,
								INSERT_TIME) 
					VALUES(#{uuid},
							#{galleryTitle},
							#{galleryFileUrl},
							SYSDATE)
	</insert>
	
	<select id="selectGallery" 
	        parameterType="String"
	        resultType="GalleryVO"
	>
		SELECT * FROM TB_GALLERY
		WHERE UUID = #{uuid}
	</select>
		
	<delete id="delete" parameterType="String">
		DELETE TB_GALLERY
		WHERE UUID = #{uuid}
	</delete>
</mapper>



